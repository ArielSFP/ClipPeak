# =============================================================================
# ClipPeak - Requirements File (PINNED VERSIONS)
# =============================================================================

# -----------------------------------------------------------------------------
# Web Framework & API (api.py)
# -----------------------------------------------------------------------------
fastapi==0.121.1
uvicorn[standard]==0.38.0
python-multipart==0.0.20
supabase==2.24.0
google-cloud-storage==2.18.2

# -----------------------------------------------------------------------------
# Deep Learning & ML Models
# -----------------------------------------------------------------------------
# NOTE: torch / torchvision / torchaudio are installed in Dockerfile
# from the CUDA 12.1 index with explicit versions:
#   torch==2.5.1+cu121
#   torchvision==0.20.1+cu121
#   torchaudio==2.5.1+cu121
#torch==2.5.1+cu121
#torchvision==0.20.1+cu121
#torchaudio==2.5.1+cu121

# -----------------------------------------------------------------------------
# Speech Recognition & Transcription
# -----------------------------------------------------------------------------
openai-whisper==20240930
faster-whisper==1.2.1
# Pulled transitively but pinned here for reproducibility:
#ctranslate2==4.6.1
openai==2.7.2

# Audio processing (required by TalkNet)
python_speech_features==0.6
soundfile==0.13.1
librosa==0.11.0

# Hugging Face for Hebrew model support
transformers==4.57.1
huggingface-hub==0.36.0

# -----------------------------------------------------------------------------
# Computer Vision & Video Processing
# -----------------------------------------------------------------------------
opencv-contrib-python==4.11.0.86
mediapipe==0.10.21

# -----------------------------------------------------------------------------
# Video/Audio Processing
# -----------------------------------------------------------------------------
ffmpeg-python==0.2.0
pytube==15.0.0
scenedetect[opencv]==0.6.7.1
imageio[ffmpeg]==2.37.2
vidgear[core]==0.3.4

# -----------------------------------------------------------------------------
# Scientific Computing
# -----------------------------------------------------------------------------
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.2
pandas==2.3.3
python-dotenv==1.2.1

# -----------------------------------------------------------------------------
# TalkNet/Fast-ASD Dependencies
# -----------------------------------------------------------------------------
filterpy==1.4.5
lap==0.5.12
sortedcontainers==2.4.0
supervision==0.26.1

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
tqdm==4.67.1
requests==2.32.5

# -----------------------------------------------------------------------------
# Auto Subtitle (installed from local directory)
# -----------------------------------------------------------------------------
# Installed with: pip install -e reelsfy_folder/utils/auto-subtitle/
# (Handled in Dockerfile)

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - Versions are pinned to what was actually installed in the build logs.
# - Torch stack is pinned in Dockerfile, not here, to keep using CUDA wheels.
# =============================================================================
